#include<stdio.h>
#include<stdlib.h>
int main(){
	FILE *fp=NULL;
/*	int a=10;
	fp=fopen("arr1.c","r+");
	fprintf(fp,"hello world");
	char ch='a';
	fputc(ch,fp);
	char str[50]="usha sri";
	fputs(str,fp);
	fprintf(fp,"%d %d",a,str);
	fclose(fp);   */
/*	char str[50],ch='a';
	fp=fopen("abd.txt","w");
	if(fp==NULL){
		printf("errror");
		exit(1);						````//to write to a file
	}
	printf("enter the string");
	gets(str);
	fputc(ch,fp);
	fclose(fp);  */
}