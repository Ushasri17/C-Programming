#include<stdio.h>
#include<string.h>
int main(){
	char str[100],sub[50];
	int n,i,j;
	printf("enter string");
	gets(str);
	printf("enter sub string");
	n=strlen(str);
	gets(sub);
	for(i=0;i<n;i++){
		
			
		}
	
}