#include<stdio.h>
#include<math.h>
int main()
{
/*	int a;
	scanf("%d",&a); 
	printf("hi%d",a*2);
	printf("\nsize of int is %d",sizeof(int));
	printf("\nsize of double%d",sizeof(double));			//memory of datatypes
	printf("\nsize of float%d",sizeof(float));
	printf("\nsize of unsigned char %d",sizeof(unsigned char));
	printf("\nsize of long  int%d",sizeof(long int));
	int a;
	scanf("%d",&a);
	if (a%2==0)
	{												//check even or odd
			printf("even");
	}
	else
	{printf("%d is odd",a);
	}
	int a;
	int b;
	scanf("%d",&a);						//swapping without 3rd variable
	scanf("%d",&b);
	printf(" result before swapping is%d,%d",a,b)	;
	a=a+b;
	b=a-b;
	a=a-b;
	printf("\n result after swapping is%d,%d",a,b)	;
	int a;
	int b;
	int c;
	scanf("%d",&a);						//swapping with 3rd variable
	scanf("%d",&b);
	printf(" result before swapping is%d,%d",a,b)	;
	c=b;
	b=a;
	a=c;
	printf("\n result after swapping is%d,%d",a,b)	;
	int a;
	scanf("%d",&a);
	if(a>0)
	{printf("%d is positive",a);}  					//check +ve/-ve/0
	else if(a<0)
	{printf("%d is negative",a);
	}
	else
	{printf("%d is zero",a);}
	int a,b ;
	scanf("%d %d",&a,&b);	
	printf("addition of%d and %d is %d\n",a,b,a+b);
	printf("substraction of%d and %d is %d\n",a,b,a-b);
	printf("multiplication of%d and %d is %d\n",a,b,a*b);		//arithmetic operations
	printf("division of%d and %d is %d\n",a,b,a/b);
	printf("modulus of%d and %d is%d\n",a,b,a%b);
	printf("%dpower  %d is%d\n",a,b,pow(a,b)); 
	int s,i;
	printf("enter a num");
	scanf("%d",&s);
	for(i=1;i<=s;i+=1)									//perfect square
	{
		if(s/i==i)
		   	printf("%d is perfect square",s);
	}*/
		  int c;
		  c=pow(4,3); 	
printf("power is %d",c);
	
}
